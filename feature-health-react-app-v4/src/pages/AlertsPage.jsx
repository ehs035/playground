import TopBar from '../components/TopBar'; import StatusPill from '../components/StatusPill'; import { useAsyncData } from '../hooks/useAsyncData'; import { getAlerts } from '../services/appService'; export default function AlertsPage(){const {data}=useAsyncData(getAlerts,[]); return <><TopBar title='Operational Alerts' subtitle='Mocked alerts for network timeouts, slow responses, and feature-specific operational issues.' right={<div className='rounded-2xl border border-white/10 bg-white/5 px-4 py-3 text-sm text-slate-300'>{data.length} active alerts</div>} /><div className='space-y-5 px-6 py-8 sm:px-8'>{data.map(alert=><div key={alert.id} className='panel p-5'><div className='flex flex-col gap-4 md:flex-row md:items-start md:justify-between'><div><div className='flex items-center gap-3'><h3 className='text-lg font-semibold'>{alert.feature}</h3><StatusPill label={alert.severity} tone={alert.severity==='critical'?'bad':'warning'} small /></div><p className='mt-3 text-sm leading-6 text-slate-300'>{alert.message}</p><div className='mt-4 flex flex-wrap gap-3 text-xs text-slate-500'><span>{alert.app}</span><span>•</span><span>{alert.type}</span><span>•</span><span>{alert.detectedAt}</span></div></div></div></div>)}</div></>}