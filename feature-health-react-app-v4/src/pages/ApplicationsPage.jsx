import { useMemo, useState } from 'react'; import { appData, statusMeta } from '../data/apps'; import TopBar from '../components/TopBar'; import SearchBar from '../components/SearchBar'; import Tabs from '../components/Tabs'; import AppOverviewTile from '../components/AppOverviewTile'; const tabs=[{value:'all',label:'All apps'},{value:'good',label:'Healthy'},{value:'warning',label:'Degraded'},{value:'bad',label:'Critical'}]; export default function ApplicationsPage(){const [q,setQ]=useState(''); const [filter,setFilter]=useState('all'); const filtered=useMemo(()=>appData.filter(app=>(filter==='all'||app.status===filter)&&(`${app.name} ${app.owner} ${app.shortName}`.toLowerCase().includes(q.toLowerCase()))),[q,filter]); return <><TopBar title='Applications' subtitle='Search, filter, and open the application-specific health pages.' right={<div className='rounded-2xl border border-white/10 bg-white/5 px-4 py-3 text-sm text-slate-300'>{filtered.length} applications</div>} /><div className='space-y-6 px-6 py-8 sm:px-8'><section className='grid gap-4 xl:grid-cols-[1fr_auto] xl:items-center'><SearchBar value={q} onChange={setQ} placeholder='Search applications...' /><Tabs tabs={tabs} active={filter} onChange={setFilter} /></section><section className='grid gap-5 lg:grid-cols-2 2xl:grid-cols-3'>{filtered.map(app=><AppOverviewTile key={app.id} app={app} meta={statusMeta[app.status]} />)}</section></div></>}